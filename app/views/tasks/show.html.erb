<h2><%= @task.title %></h2><h2> - <%= @task.completed %></h2><p>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%></p>
<p><%= @task.details %></p>
<%= link_to "Edit", edit_path(@task) %>
<%= link_to "Back to the list", tasks_path %>
